# wings
A JavaScript and TypeScript runtime built on V8

```js
import { serve } from "https://github.com/timo-cmd2/wings/std/server.js"
const s = serve({ port: 8000 });

/** respond to the server with message */
req.respond({ body: "Hello World\n" });```
